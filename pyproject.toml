[project]
name = "ReLax"
version = "0.1.0"
dependencies = [
  "tiktoken",
  "blobfile",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["*"]

[tool.pytest.ini_options]
pythonpath = [
  "."
] 